# Simple-Delivery-Allocation-Load-Balancing
A delivery company has 6 packages (P1–P6) to be delivered from a single Depot by 2 vehicles. The objective  is to split the packages so that each vehicle travels roughly the same total distance — achieving load balance. 
